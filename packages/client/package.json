{
    "name": "@regex-radar/client",
    "private": true,
    "displayName": "Regex Radar Language Server Client",
    "publisher": "regex-radar",
    "description": "The Language Server Client for Regex Radar",
    "version": "0.1.3",
    "type": "module",
    "exports": {
        ".": {
            "types": "./dist/index.d.ts",
            "import": "./dist/index.js"
        }
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/regex-radar/regex-radar",
        "directory": "packages/client"
    },
    "homepage": "https://github.com/regex-radar/regex-radar",
    "scripts": {
        "build": "node scripts/build.js",
        "dev": "node scripts/build.js --watch",
        "build:production": "node scripts/build.js --production",
        "tsc": "tsc -b tsconfig.json",
        "test": "vitest run",
        "test:dev": "vitest",
        "clean": "rm -r dist/"
    },
    "dependencies": {
        "@regex-radar/protocol": "*",
        "vscode-languageclient": "^9.0.1"
    },
    "devDependencies": {
        "esbuild": "^0.27.2",
        "vitest": "^4.0.16"
    }
}
