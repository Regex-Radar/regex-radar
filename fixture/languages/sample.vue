<script lang="js">
const literal = /pattern/g;

const fn = RegExp('pattern', 'g');

const new_expression = new RegExp('pattern', 'g');
</script>
<script lang="jsx">
const literal = /pattern/g;

const fn = RegExp('pattern', 'g');

const new_expression = new RegExp('pattern', 'g');
</script>
<script lang="ts">
const literal = /pattern/g;

const fn = RegExp('pattern', 'g');

const new_expression = new RegExp('pattern', 'g');
</script>
<script lang="tsx">
const literal = /pattern/g;

const fn = RegExp('pattern', 'g');

const new_expression = new RegExp('pattern', 'g');
</script>